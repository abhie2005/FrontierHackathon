# FalkorDB connection.
# LOCAL DOCKER (default): docker run -p 6379:6379 -it falkordb/falkordb:latest
# FALKORDB CLOUD: copy the connection details from your free instance dashboard.
FALKORDB_HOST=localhost
FALKORDB_PORT=6379
FALKORDB_USERNAME=
FALKORDB_PASSWORD=
FALKORDB_GRAPH=warehouse

# RocketRide. With an API key set, the executor makes a real authenticated
# connection to the RocketRide cloud runtime. Leave ROCKETRIDE_API_KEY empty to
# fall back to local logs-only mode.
ROCKETRIDE_API_KEY=
ROCKETRIDE_URI=https://api.rocketride.ai

# LaserData — real-time signal layer (Apache Iggy transport).
# Get a free connection string from https://laserdata.cloud (no credit card).
# Format is bare user:password@host:port, e.g. iggy:iggy@your-host.laserdata.cloud:8090
LASER_CONNECTION_STRING=
LASER_STREAM=hopper

# Anthropic API key — only needed if you enable LLM-generated rationale (LLM_RATIONALE=1).
# Threshold logic works fully without it.
ANTHROPIC_API_KEY=
LLM_RATIONALE=0
